const express = require('express');
const app = express();

//settings
/*puerto definido variable a las que toda la aplicacion puede tener acceso*/
app.set('port',4000)

//middlewares
 
//routes

module.exports = app;
